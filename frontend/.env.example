# Frontend environment configuration
# The API origin should NOT include the /api/suppliers path.
# Leave this commented if you are running the Vite dev server on port 5173 with the
# Django backend on http://localhost:8000 (that combination is detected automatically).
# VITE_SUPPLIERS_API_ORIGIN=http://localhost:8000
